apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: postgres-pdb
  namespace: promptcontent-dev
  labels:
    app: postgres
    tier: database
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: postgres
