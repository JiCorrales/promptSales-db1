apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres
  namespace: promptcontent-dev
data:
  # Configuraciones no sensibles - Las credenciales están en postgres-secret
  POSTGRES_DB: "PromptContent"
