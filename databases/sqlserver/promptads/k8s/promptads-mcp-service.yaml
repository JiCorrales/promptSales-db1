apiVersion: v1
kind: Service
metadata:
  name: promptads-mcp-service
  namespace: promptads
spec:
  type: ClusterIP
  selector:
    app: promptads-mcp-server
  ports:
  - name: http
    port: 8080        # puerto lógico dentro del cluster
    targetPort: 8080  # puerto expuesto por el contenedor
    protocol: TCP
